export const COMMON = true;

export enum GameMode {
    GEOQUIZZ,
    IMPOSTEUR,
    INFILTRER,
    KCULTURE,
    SPYFALL,
}

export enum LobbyStep {
    PLAYERS,
    SELECT,
    CONFIGURATION,
    GAME,
    RESULT
}